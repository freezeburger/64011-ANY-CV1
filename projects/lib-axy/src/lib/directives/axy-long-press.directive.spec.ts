import { AxyLongPressDirective } from './axy-long-press.directive';

describe('AxyLongPressDirective', () => {
  it('should create an instance', () => {
    const directive = new AxyLongPressDirective();
    expect(directive).toBeTruthy();
  });
});
