/**
 * Appearances of the button
 * 'primary' is the default appearance
 * 'outline' is for outline buttons
 * 'ghost' is for ghost buttons (no background, no border)
 *
 * Apply to buttons, inputs, selects, textareas, etc.
 */
export type Appearances = 'primary' | 'outline' | 'ghost';

/**
 * Sizes within the design system
 * 'md' is the default size
 * 'sm' is for small
 * 'lg' is for large
 *
 * Applyed to buttons, inputs, selects, textareas, etc.
 */
export type Sizes = 'sm' | 'md' | 'lg';

/**
 * Colors for various components
 */
export type Colors = 'crimson' | 'lightgreen' | 'orange'
